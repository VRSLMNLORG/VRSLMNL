# 🎮 Описание механик игры

Документ содержит полное техническое описание всех игровых механик и используемых скриптов.

---

## 📘 1. Механика принудительной перспективы (Forced Perspective System)

### Описание
Основная механика игры, реализующая систему захвата и удержания объектов с принудительной перспективой. Объект, находящийся в руках игрока, сохраняет свой визуальный размер на экране независимо от расстояния до камеры. При этом предмет автоматически смещается вплотную к ближайшей видимой поверхности, создавая иллюзию масштабного изменения (как в игре Superliminal).
Это механика использует движение шлема.

### Используемые скрипты

#### Основной класс: `ForcedPerspectiveFromPickup`
**Расположение:** `ForcedPerspective/ForcedPerspectiveFromPickup.cs`

#### Частичный класс: `ForcedPerspectiveFromPickup.InputAndUpdate`
**Расположение:** `ForcedPerspective/ForcedPerspectiveFromPickup.InputAndUpdate.cs`

#### Частичный класс: `ForcedPerspectiveFromPickup.Orientation`
**Расположение:** `ForcedPerspective/ForcedPerspectiveFromPickup.Orientation.cs`

#### Частичный класс: `ForcedPerspectiveFromPickup.Grid`
**Расположение:** `ForcedPerspective/ForcedPerspectiveFromPickup.Grid.cs`

#### Частичный класс: `ForcedPerspectiveFromPickup.PlacementAndScale`
**Расположение:** `ForcedPerspective/ForcedPerspectiveFromPickup.PlacementAndScale.cs`

### Принцип работы

1. **Выбор и захват объекта:**
   - При одновременном нажатии двух триггеров выполняется лучевой тест от камеры
   - Проверяется прямая видимость объекта (нет препятствий между камерой и объектом)
   - Если луч пересекает объект с компонентом `ForcedPerspectiveFromPickup`, начинается захват
   - Физика объекта отключается (Rigidbody становится kinematic)

2. **Центрирование в экране:**
   - После захвата объект плавно смещается в центр экрана (или заданную viewport-позицию)
   - Используется корутина `CenterViewportCoroutine()` с настраиваемой кривой анимации
   - В дальнейшем объект остаётся привязанным к экранным координатам

3. **Определение расстояния до препятствий:**
   - Строится силуэтная сетка из точек, формирующих контур объекта
   - Из каждой точки сетки пускается луч в направлении камеры
   - Находится минимальная дистанция до препятствий
   - Объект перемещается вплотную перед ближайшей геометрией

4. **Поддержание масштаба и ориентации:**
   - Масштаб вычисляется динамически: `scale = distance / originalDistanceToScaleRatio`
   - Объект выравнивается по вертикали мира (опция `keepUpright`)
   - Опционально поворачивается в сторону взгляда камеры (`followCameraYaw`)

5. **Отпускание предмета:**
   - При отпускании триггеров объект возвращается к обычной физике
   - Восстанавливаются исходные параметры (гравитация, слои коллайдеров, родитель)
   - Вызывается событие `HoldingEnded` для уведомления других систем

### Геймплейные применения

- **Манипуляция размерами объектов:** Игрок может визуально изменять размер объектов, приближая их или удаляя относительно камеры, создавая иллюзию увеличения или уменьшения
- **Открытие путей:** Увеличенные объекты могут перекрывать проходы или активировать кнопки
- **Активация механизмов:** Изменение размера позволяет взаимодействовать с различными интерактивными элементами

---

## 🎯 2. Система прицела и подсветки (Camera Reticle System)

### Описание
Визуальная система, отображающая прицел в центре камеры и подсвечивающая объекты при наведении. Интегрируется с системой принудительной перспективы для улучшения обратной связи с игроком.

### Используемые скрипты

#### `CameraReticleSimple`
**Расположение:** `CameraReticleSimple.cs`

**Режимы отображения прицела:**
- `Generated` — кольцо генерируется программно в `OnGUI`
- `CustomTexture` — используется пользовательская текстура

**Режимы видимости:**
- `Always` — прицел всегда виден
- `OnHover` — виден только при наведении на объект
- `WhileActionPressed` — виден только при удержании действия

**Режимы подсветки:**
- `SwapMaterial` — замена материала на glow-материал
- `Emission` — включение эмиссии с заданным цветом и интенсивностью

**Особенности:**
- Поддержка отображения прицела без цели (с уменьшенной прозрачностью)
- Подавление мерцания при быстром захвате/отпускании

---

## 🚪 3. Система интерактивных объектов (Interactables System)

### Описание
Набор компонентов для создания интерактивных элементов окружения: кнопки, двери, сокеты. Все компоненты поддерживают события Unity и могут быть связаны через Inspector.

### Используемые скрипты

#### 3.1. `FloorButton`
**Расположение:** `interactablesObject/FloorButton.cs`

**Описание:** Напольная кнопка (pressure plate), срабатывающая от объектов, наступающих на неё.

**Режимы активации:**
- `AnyCollider` — любой коллайдер активирует кнопку
- `RequireRigidbodyMinMass` — требуется Rigidbody с минимальной массой (Пока это не реализовано полностью)

**Применение:**
- Активация механизмов при размещении объектов на кнопке
- Использование увеличенных объектов для нажатия кнопок (механика принудительной перспективы, то есть увеличеный размер)

#### 3.2. `SocketButton`
**Расположение:** `interactablesObject/SocketButton.cs`

**Описание:** Кнопка на основе XR Socket Interactor из XR Interaction Toolkit. Срабатывает при вставке объектов в сокет.

**Режимы работы:**
- `HoldWhileSeated` — OnPressed при вставке, OnReleased при извлечении
- `ToggleOnSeat` — переключение состояния при каждой вставке

**Требования:**
- Компонент `XRSocketInteractor` на том же объекте

**Применение:**
- Создание пазов для размещения объектов
- Активация механизмов при правильном размещении предметов

3.3. `RotatingDoorWithCarryBlocker`
**Расположение:** `interactablesObject/RotatingDoorWithCarryBlocker.cs`

**Описание:** Вращающаяся дверь с интегрированной системой препятствий. Дверь вращается вокруг оси и управляет состоянием CarryBlocker в проёме.

**События:**
- `OnOpened` — вызывается при полном открытии
- `OnClosed` — вызывается при полном закрытии

**Визуальные опции:**
- `hideRenderersWhenOpen` — скрывать рендереры при открытии
- `setDoorCollidersTriggerWhenOpen` — делать коллайдеры двери триггерами при открытии

**Применение:**
- Создание проходов, которые можно открывать/закрывать
- Блокировка проноса объектов через закрытые двери

#### 3.4. `SlidingDoorWithCarryBlocker`
**Расположение:** `interactablesObject/SlidingDoorWithCarryBlocker.cs`

**Описание:** Раздвижная дверь с интегрированной системой препятствий. Аналогична вращающейся двери, но использует линейное перемещение вместо вращения.

---

## 🛡️ 4. Система препятствий (Carry Blocker System) (Не до конца реализован)

### Описание
Система управления коллайдерами-препятствиями в дверных проёмах. Препятствия становятся твёрдыми (solid) или триггерами в зависимости от состояния двери и наличия удерживаемых объектов. Предотвращает пронос объектов через закрытые двери и блокирует телепортацию.

### Используемые скрипты

#### `CarryBlockerToggleOnGrab`
**Расположение:** `interactablesObject/CarryBlockerToggleOnGrab.cs`

**Применение:**
- Блокировка проноса объектов через закрытые двери
- Предотвращение телепортации через закрытые проёмы
- Динамическое управление проходимостью в зависимости от игрового состояния

---

## 🚶 5. Система безопасности телепортации (Teleport Landing Safety)

### Описание
Предотвращает застревание игрока в препятствиях после телепортации. Обнаруживает большие перемещения (телепортацию) и автоматически отталкивает CharacterController от препятствий, сохраняя безопасный зазор.

### Используемые скрипты

#### `TeleportLandingSafety`
**Расположение:** `interactablesObject/TeleportLandingSafety.cs`

**Применение:**
- Предотвращение застревания в дверях после телепортации
- Коррекция позиции при неточных телепортациях
- Улучшение комфорта игры в VR
