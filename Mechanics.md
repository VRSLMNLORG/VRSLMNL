# 🎮 Описание механик игры

Документ содержит полное техническое описание всех игровых механик и используемых скриптов.

---

## 📘 1. Механика принудительной перспективы (Forced Perspective System)

### Описание
Основная механика игры, реализующая систему захвата и удержания объектов с принудительной перспективой. Объект, находящийся в руках игрока, сохраняет свой визуальный размер на экране независимо от расстояния до камеры. При этом предмет автоматически смещается вплотную к ближайшей видимой поверхности, создавая иллюзию масштабного изменения (как в игре Superliminal).

### Используемые скрипты

#### Основной класс: `ForcedPerspectiveFromPickup`
**Расположение:** `ForcedPerspective/ForcedPerspectiveFromPickup.cs`

**Ответственность:**
- Управление жизненным циклом захвата (StartHolding/ReleaseHolding)
- Глобальные события `HoldingStarted` и `HoldingEnded` для синхронизации с другими системами
- Проверка прямой видимости объекта перед захватом
- Создание и управление ProxyCollider для точного определения границ объекта
- Плавная отцентровка объекта в экране при захвате
- Управление физикой объекта (Rigidbody) во время удержания
- Временное изменение слоёв коллайдеров для предотвращения конфликтов с лучами

**Ключевые параметры:**
- `gazeMaxDistance` — максимальная дистанция взгляда для Захвата
- `centerOnPickup` — включение плавной отцентровки при захвате
- `centeringDuration` — длительность анимации отцентровки
- `targetViewportOnPickup` — целевая позиция на экране (0..1)
- `keepUpright` — поддержание вертикальной ориентации
- `followCameraYaw` — поворот объекта в сторону взгляда камеры
- `yawFollowSpeed` — скорость следования по рысканью

#### Частичный класс: `ForcedPerspectiveFromPickup.InputAndUpdate`
**Расположение:** `ForcedPerspective/ForcedPerspectiveFromPickup.InputAndUpdate.cs`

**Ответственность:**
- Обработка ввода через Unity Input System (левая/правая рука)
- Кэширование луча взгляда один раз на кадр для предотвращения гонок
- Детекция одновременного нажатия двух триггеров для начала захвата
- Вызов обновления позиции, ориентации и масштаба в `OnBeforeRenderCallback` (синхронизация с HMD)

**Ключевые методы:**
- `RefreshGazeCache()` — кэширование попадания взгляда
- `BothHandsPressed()` — проверка одновременного нажатия
- `Update()` — основная логика захвата/отпускания
- `OnBeforeRenderCallback()` — обновление перед рендерингом

#### Частичный класс: `ForcedPerspectiveFromPickup.Orientation`
**Расположение:** `ForcedPerspective/ForcedPerspectiveFromPickup.Orientation.cs`

**Ответственность:**
- Вычисление целевой ориентации объекта с учётом вертикали и направления камеры
- Плавное сглаживание поворота при необходимости

**Ключевые методы:**
- `UpdateOrientationSmooth()` — обновление ориентации с плавным переходом
- `ComputeUprightTargetRotation()` — расчёт целевого поворота

#### Частичный класс: `ForcedPerspectiveFromPickup.Grid`
**Расположение:** `ForcedPerspective/ForcedPerspectiveFromPickup.Grid.cs`

**Ответственность:**
- Построение "силуэтной сетки" — набора точек, формирующих контур объекта
- Поддержка двух типов сеток: прямоугольной и полярной (эллиптической)
- Фильтрация точек сетки: остаются только те, через которые луч от камеры пересекает объект

**Ключевые методы:**
- `GetBoundingBoxPoints()` — получение 8 углов bounding box объекта
- `SetupShapedGrid()` — основная функция построения сетки
- `GetRectConfines()` — определение границ проекции объекта в локальном пространстве камеры
- `SetupGrid()` — создание прямоугольной сетки
- `SetupPolarEllipseGrid()` — создание полярной сетки (кольца и секторы)
- `GetShapedGrid()` — фильтрация точек сетки по пересечению с коллайдерами объекта

**Параметры сетки:**
- `NUMBER_OF_GRID_ROWS` / `NUMBER_OF_GRID_COLUMNS` — размеры прямоугольной сетки
- `usePolarGrid` — использование полярной сетки вместо прямоугольной
- `polarRings` — количество колец в полярной сетке
- `polarSectors` — количество секторов в полярной сетке
- `edgeBias` — смещение плотности точек к краям (>1 — больше точек у контура)

#### Частичный класс: `ForcedPerspectiveFromPickup.PlacementAndScale`
**Расположение:** `ForcedPerspective/ForcedPerspectiveFromPickup.PlacementAndScale.cs`

**Ответственность:**
- Позиционирование объекта вплотную перед ближайшими препятствиями
- Поддержание постоянного видимого размера объекта (масштабирование по дистанции)
- Сохранение фиксированной позиции объекта на экране (viewport anchor)

**Ключевые методы:**
- `MoveInFrontOfObstacles()` — перемещение объекта перед препятствиями
  - Трассирует лучи от камеры через все точки силуэтной сетки
  - Находит минимальную дистанцию до препятствий
  - Учитывает толщину объекта для предотвращения проникновения в стены
- `UpdateScale()` — динамическое масштабирование для сохранения видимого размера
  - Масштаб = текущая дистанция / исходное отношение дистанции к масштабу
- `MaintainViewportAnchor()` — поддержание фиксированной позиции на экране
- `ComputeHalfThicknessAlongPredicted()` — вычисление половины толщины объекта вдоль направления взгляда
- `CalculateCompositeLocalBounds()` — расчёт композитных границ всех дочерних рендереров

**Параметры:**
- `_wallLayers` — слои препятствий (стены/сцена)
- `proxyPadding` — относительный запас к размерам прокси-коллайдера

### Принцип работы

1. **Выбор и захват объекта:**
   - При одновременном нажатии двух триггеров выполняется лучевой тест от камеры
   - Проверяется прямая видимость объекта (нет препятствий между камерой и объектом)
   - Если луч пересекает объект с компонентом `ForcedPerspectiveFromPickup`, начинается захват
   - Физика объекта отключается (Rigidbody становится kinematic)

2. **Центрирование в экране:**
   - После захвата объект плавно смещается в центр экрана (или заданную viewport-позицию)
   - Используется корутина `CenterViewportCoroutine()` с настраиваемой кривой анимации
   - В дальнейшем объект остаётся привязанным к экранным координатам

3. **Определение расстояния до препятствий:**
   - Строится силуэтная сетка из точек, формирующих контур объекта
   - Из каждой точки сетки пускается луч в направлении камеры
   - Находится минимальная дистанция до препятствий
   - Объект перемещается вплотную перед ближайшей геометрией с учётом его толщины

4. **Поддержание масштаба и ориентации:**
   - Масштаб вычисляется динамически: `scale = distance / originalDistanceToScaleRatio`
   - Объект выравнивается по вертикали мира (опция `keepUpright`)
   - Опционально поворачивается в сторону взгляда камеры (`followCameraYaw`)

5. **Отпускание предмета:**
   - При отпускании триггеров объект возвращается к обычной физике
   - Восстанавливаются исходные параметры (гравитация, слои коллайдеров, родитель)
   - Вызывается событие `HoldingEnded` для уведомления других систем

### Геймплейные применения

- **Манипуляция размерами объектов:** Игрок может визуально изменять размер объектов, приближая их или удаляя относительно камеры, создавая иллюзию увеличения или уменьшения
- **Открытие путей:** Увеличенные объекты могут перекрывать проходы или активировать кнопки
- **Активация механизмов:** Изменение размера позволяет взаимодействовать с различными интерактивными элементами

---

## 🎯 2. Система прицела и подсветки (Camera Reticle System)

### Описание
Визуальная система, отображающая прицел в центре камеры и подсвечивающая объекты при наведении. Интегрируется с системой принудительной перспективы для улучшения обратной связи с игроком.

### Используемые скрипты

#### `CameraReticleSimple`
**Расположение:** `CameraReticleSimple.cs`

**Ответственность:**
- Отображение прицела в центре экрана
- Определение объектов под взглядом через raycast
- Подсветка объектов при наведении
- Синхронизация видимости прицела с состоянием захвата

**Режимы отображения прицела:**
- `Generated` — кольцо генерируется программно в `OnGUI`
- `CustomTexture` — используется пользовательская текстура

**Режимы видимости:**
- `Always` — прицел всегда виден
- `OnHover` — виден только при наведении на объект
- `WhileActionPressed` — виден только при удержании действия

**Режимы подсветки:**
- `SwapMaterial` — замена материала на glow-материал
- `Emission` — включение эмиссии с заданным цветом и интенсивностью

**Ключевые параметры:**
- `gazeMaxDistance` — максимальная дистанция взгляда
- `raycastMask` — слои для raycast
- `requirePickupComponent` — требовать компонент `ForcedPerspectiveFromPickup` для обнаружения
- `obstacleLayers` — слои препятствий, блокирующих взгляд
- `holdActionLeft` / `holdActionRight` — действия ввода для удержания
- `suppressOnPickup` — подавление прицела после начала захвата (избежание мерцания)

**Интеграция:**
- Подписывается на события `ForcedPerspectiveFromPickup.HoldingStarted` и `HoldingEnded`
- Автоматически скрывает прицел и подсветку при начале захвата
- Восстанавливает отображение после отпускания

**Особенности:**
- Поддержка отображения прицела без цели (с уменьшенной прозрачностью)
- Подавление мерцания при быстром захвате/отпускании
- Кэширование сгенерированной текстуры кольца для оптимизации

---

## 🚪 3. Система интерактивных объектов (Interactables System)

### Описание
Набор компонентов для создания интерактивных элементов окружения: кнопки, двери, сокеты. Все компоненты поддерживают события Unity и могут быть связаны через Inspector.

### Используемые скрипты

#### 3.1. `FloorButton` (НЕОБХОДИМО ИНТЕГРИРОВАТЬ В ПЕРСПЕКТИВУ И ИЗМЕНЕНИЕ МАССЫ)
**Расположение:** `interactablesObject/FloorButton.cs`

**Описание:** Напольная кнопка (pressure plate), срабатывающая от объектов, наступающих на неё.

**Ответственность:**
- Детекция объектов в триггерной зоне
- Фильтрация по слоям и типу активатора
- Визуальная анимация нажатия (опционально)
- Генерация событий OnPressed/OnReleased

**Режимы активации:**
- `AnyCollider` — любой коллайдер активирует кнопку
- `RequireRigidbodyMinMass` — требуется Rigidbody с минимальной массой

**Параметры:**
- `activatorLayers` — слои объектов, которые могут нажимать кнопку
- `minMass` — минимальная масса для режима `RequireRigidbodyMinMass`
- `buttonTop` — Transform верхней части кнопки для визуальной анимации
- `pressedLocalOffset` — локальное смещение при нажатии
- `pressLerpSpeed` — скорость анимации нажатия

**Применение:**
- Активация механизмов при размещении объектов на кнопке
- Использование увеличенных объектов для нажатия кнопок (механика принудительной перспективы)

#### 3.2. `SocketButton`
**Расположение:** `interactablesObject/SocketButton.cs`

**Описание:** Кнопка на основе XR Socket Interactor из XR Interaction Toolkit. Срабатывает при вставке объектов в сокет.

**Ответственность:**
- Мониторинг состояния XR Socket Interactor
- Фильтрация по XR Interaction Layers
- Генерация событий в зависимости от режима

**Режимы работы:**
- `HoldWhileSeated` — OnPressed при вставке, OnReleased при извлечении
- `ToggleOnSeat` — переключение состояния при каждой вставке

**Параметры:**
- `socket` — ссылка на XRSocketInteractor (автоматически находится на этом объекте)
- `requiredInteractionLayers` — требуемые XR Interaction Layers (0 = игнорировать фильтр)

**Требования:**
- Компонент `XRSocketInteractor` на том же объекте

**Применение:**
- Создание пазов для размещения объектов
- Активация механизмов при правильном размещении предметов

#### 3.3. `RotatingDoorWithCarryBlocker`
**Расположение:** `interactablesObject/RotatingDoorWithCarryBlocker.cs`

**Описание:** Вращающаяся дверь с интегрированной системой препятствий. Дверь вращается вокруг оси и управляет состоянием CarryBlocker в проёме.

**Ответственность:**
- Анимация вращения двери вокруг оси
- Управление визуальным состоянием (скрытие рендереров, переключение коллайдеров)
- Интеграция с `CarryBlockerToggleOnGrab` для управления препятствиями
- Автоматическое закрытие через заданное время (опционально)

**Режимы работы:**
- **Иерархический шарнир:** Вращение через изменение `localRotation` дочернего Transform
- **Виртуальный шарнир:** Вращение вокруг заданной точки в локальном пространстве

**Параметры:**
- `doorPivot` — Transform, который будет вращаться
- `rotateTarget` — целевой объект для вращения (классический pivot)
- `localAxis` — локальная ось вращения (обычно Y = вверх)
- `openAngle` — угол поворота от закрытого до открытого состояния (градусы)
- `moveTime` — время полного открытия/закрытия (секунды)
- `ease` — кривая анимации (easing)
- `autoCloseDelay` — автоматическое закрытие через N секунд (0 = отключено)
- `useHingeOffset` — использование виртуального шарнира
- `hingeLocalPoint` — точка шарнира в локальном пространстве
- `carryBlocker` — ссылка на объект с `CarryBlockerToggleOnGrab`

**События:**
- `OnOpened` — вызывается при полном открытии
- `OnClosed` — вызывается при полном закрытии

**Визуальные опции:**
- `hideRenderersWhenOpen` — скрывать рендереры при открытии
- `setDoorCollidersTriggerWhenOpen` — делать коллайдеры двери триггерами при открытии

**Применение:**
- Создание проходов, которые можно открывать/закрывать
- Блокировка проноса объектов через закрытые двери

#### 3.4. `SlidingDoorWithCarryBlocker`
**Расположение:** `interactablesObject/SlidingDoorWithCarryBlocker.cs`

**Описание:** Раздвижная дверь с интегрированной системой препятствий. Аналогична вращающейся двери, но использует линейное перемещение вместо вращения.

**Ответственность:**
- Анимация линейного перемещения двери
- Управление визуальным состоянием
- Интеграция с `CarryBlockerToggleOnGrab`

**Параметры:**
- `movingPart` — Transform, который будет перемещаться
- `openLocalOffset` — локальное смещение от закрытого до открытого состояния
- `moveTime` — время полного открытия/закрытия
- `ease` — кривая анимации
- `autoCloseDelay` — автоматическое закрытие
- `carryBlocker` — ссылка на объект с `CarryBlockerToggleOnGrab`

**Отличия от RotatingDoor:**
- Использует `localPosition` вместо `localRotation`
- Проще в настройке (не требует расчёта углов)

---

## 🛡️ 4. Система препятствий (Carry Blocker System) (Пока в не завершен)

### Описание
Система управления коллайдерами-препятствиями в дверных проёмах. Препятствия становятся твёрдыми (solid) или триггерами в зависимости от состояния двери и наличия удерживаемых объектов. Предотвращает пронос объектов через закрытые двери и блокирует телепортацию.

### Используемые скрипты

#### `CarryBlockerToggleOnGrab`
**Расположение:** `interactablesObject/CarryBlockerToggleOnGrab.cs`

**Ответственность:**
- Управление состоянием коллайдера (solid vs trigger)
- Мониторинг состояния двери через `SetDoorOpen()`
- Отслеживание удерживаемых объектов через события `ForcedPerspectiveFromPickup`
- Опциональная интеграция с XR Interactors

**Параметры:**
- `carryBlocker` — коллайдер для управления (автоматически находится или создаётся BoxCollider)
- `ensureBoxCollider` — автоматически создать BoxCollider, если его нет
- `setObstacleLayer` — автоматически назначить слой "Obstacle"
- `solidWhenDoorClosed` — делать препятствие твёрдым, когда дверь закрыта
- `solidWhileHolding` — делать препятствие твёрдым, когда удерживается объект
- `useForcedPerspectiveEvents` — подписываться на события `ForcedPerspectiveFromPickup`
- `alsoUseXRInteractorSelections` — также учитывать XR Interactor selections
- `interactors` — список XR Interactors для мониторинга (пустой = автообнаружение)

**Методы:**
- `SetDoorOpen(bool isOpen)` — вызывается дверями для уведомления о состоянии

**Интеграция:**
- Работает с `RotatingDoorWithCarryBlocker` и `SlidingDoorWithCarryBlocker`
- Слушает события `ForcedPerspectiveFromPickup.HoldingStarted/Ended`
- Может отслеживать XR Interactor selections для совместимости с другими системами захвата

**Применение:**
- Блокировка проноса объектов через закрытые двери
- Предотвращение телепортации через закрытые проёмы
- Динамическое управление проходимостью в зависимости от игрового состояния

---

## 🚶 5. Система безопасности телепортации (Teleport Landing Safety)

### Описание
Предотвращает застревание игрока в препятствиях после телепортации. Обнаруживает большие перемещения (телепортацию) и автоматически отталкивает CharacterController от препятствий, сохраняя безопасный зазор.

### Используемые скрипты

#### `TeleportLandingSafety`
**Расположение:** `interactablesObject/TeleportLandingSafety.cs`

**Ответственность:**
- Детекция телепортации через отслеживание больших перемещений за один кадр
- Вычисление необходимого отталкивания от препятствий
- Применение коррекции через CharacterController.Move()

**Принцип работы:**
1. В `LateUpdate()` проверяется дистанция перемещения за кадр
2. Если перемещение превышает `teleportDeltaThreshold`, активируется режим коррекции на `settleFrames` кадров
3. В режиме коррекции вычисляется капсула CharacterController с учётом `safeMargin`
4. Находятся все коллайдеры, пересекающие расширенную капсулу
5. Для каждого препятствия вычисляется направление и расстояние отталкивания
6. Применяется коррекция через `CharacterController.Move()` с ограничением `maxPushPerFrame`

**Параметры:**
- `obstacleMask` — слои препятствий для учёта
- `safeMargin` — горизонтальный зазор от препятствий вокруг радиуса капсулы (метры)
- `teleportDeltaThreshold` — порог дистанции для детекции телепортации (метры)
- `settleFrames` — количество кадров для коррекции после телепортации
- `maxPushPerFrame` — максимальное отталкивание за кадр (метры, предотвращает большие скачки)

**Требования:**
- Компонент `CharacterController` на том же объекте (обычно XR Origin)

**Применение:**
- Предотвращение застревания в дверях после телепортации
- Коррекция позиции при неточных телепортациях
- Улучшение комфорта игры в VR

### Примеры интеграции

1. **Захват объекта → Блокировка двери:**
   - Игрок захватывает объект (`ForcedPerspectiveFromPickup.HoldingStarted`)
   - `CarryBlockerToggleOnGrab` получает событие и делает препятствие твёрдым
   - Объект не может быть пронесён через закрытую дверь

2. **Увеличение объекта → Активация кнопки:**
   - Игрок захватывает объект и приближает его к камере (увеличение)
   - Увеличенный объект помещается на `FloorButton`
   - Кнопка активируется и открывает дверь через события

3. **Телепортация → Безопасная посадка:**
   - Игрок телепортируется рядом с закрытой дверью
   - `TeleportLandingSafety` обнаруживает большое перемещение
   - CharacterController автоматически отталкивается от препятствия (`CarryBlockerToggleOnGrab`)

---

